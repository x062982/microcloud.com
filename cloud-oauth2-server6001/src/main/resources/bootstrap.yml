server:
  port: 6001

spring:
  application:
    name: cloud-oauth2-server
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 100ms
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
management:
  endpoint:
    health:
      enabled: true